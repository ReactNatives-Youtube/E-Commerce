import React from 'react';
import type {Node} from 'react';
import {Text, View} from 'react-native';

const App: () => Node = () => {
  return (
    <View>
      <Text>Let's Start with boilerplate</Text>
    </View>
  );
};

export default App;
